{% extends 'baseAdmin.html.twig' %}

{% block title %}Admin modifier partenaires | Trail des jambes Allaire
{% endblock %}

{% block body %}
	<h1>Modifier Partenaires</h1>

	{{ include('partners/_form.html.twig', {'button_label': 'Modifier'}) }}

	<a class="backList" href="{{ path('partners_index') }}">
		<span>Retour à la liste</span>
	</a>
	{% if  is_granted('ROLE_ADMIN') %}
		{{ include('partners/_delete_form.html.twig') }}
	{% endif %}

{% endblock %}
