{% extends 'baseAdmin.html.twig' %}

{% block title %}Admin modifier inscription | Trail des jambes Allaire{% endblock %}

{% block body %}
    <h1>MODIFIER INSCRIPTION TRAIL</h1>

    {{ include('register_trail/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a class="backList" href="{{ path('register_trail_index') }}"><span>Retour à la liste</span></a>
	{% if  is_granted('ROLE_ADMIN') %}
	  {{ include('register_trail/_delete_form.html.twig') }}
	{% endif %}
  
{% endblock %}
