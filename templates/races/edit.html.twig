{% extends 'baseAdmin.html.twig' %}

{% block title %}Admin modifier course | Trail des jambes Allaire
{% endblock %}

{% block body %}
	<h1>Modifier la course</h1>

	{{ include('races/_form.html.twig', {'button_label': 'Modifier'}) }}

	<a class="backList" href="{{ path('races_index') }}">
		<span>Retour à la liste</span>
	</a>
	{% if  is_granted('ROLE_ADMIN') %}
		{{ include('races/_delete_form.html.twig') }}
	{% endif %}

{% endblock %}
