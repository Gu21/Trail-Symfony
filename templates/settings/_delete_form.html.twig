<form method="post" action="{{ path('settings_delete', {'id': setting.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ setting.id) }}">
    <button class="btn"><i class="fas fa-trash-alt"></i></button>
</form>
