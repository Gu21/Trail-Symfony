{% extends 'baseAdmin.html.twig' %}

{% block title %}Admin modifier vidéo | Trail des jambes Allaire
{% endblock %}

{% block body %}
	<h1>MODIFIER UNE VIDEO</h1>

	{{ include('videos/_form.html.twig', {'button_label': 'Modifier'}) }}

	<a class="backList" href="{{ path('videos_index') }}">
		<span>Retour à la liste</span>
	</a>
	{% if  is_granted('ROLE_ADMIN') %}
		{{ include('videos/_delete_form.html.twig') }}
	{% endif %}

{% endblock %}
