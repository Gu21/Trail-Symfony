{% extends 'base.html.twig' %}

{% block title %}Edit Volunteers{% endblock %}

{% block body %}
    <h1>Edit Volunteers</h1>

    {{ include('volunteers/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('volunteers_index') }}">back to list</a>

    {{ include('volunteers/_delete_form.html.twig') }}
{% endblock %}
