{% extends 'base.html.twig' %}

{% block title %}Participants index{% endblock %}

{% block body %}
    <h1>Participants index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>TitleParticipant</th>
                <th>DescriptionParticipant</th>
                <th>LinkParticipant</th>
                <th>DownloadRegistrationParticipant</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for participant in participants %}
            <tr>
                <td>{{ participant.id }}</td>
                <td>{{ participant.titleParticipant }}</td>
                <td>{{ participant.descriptionParticipant }}</td>
                <td>{{ participant.linkParticipant }}</td>
                <td>{{ participant.downloadRegistrationParticipant }}</td>
                <td>
                    <a href="{{ path('participants_show', {'id': participant.id}) }}">show</a>
                    <a href="{{ path('participants_edit', {'id': participant.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('participants_new') }}">Create new</a>
{% endblock %}
