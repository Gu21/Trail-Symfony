    <form method="post" action="{{ path('register_trail_delete', {'id': register_trail.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément?');">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ register_trail.id) }}">
        <button class="btn"><i class="fas fa-trash-alt"></i></button> 
    </form>