{% extends 'baseAdmin.html.twig' %}

{% block title %}Videos{% endblock %}

{% block body %}
    <h1>Videos</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ video.id }}</td>
            </tr>
            <tr>
                <th>TitleVideo</th>
                <td>{{ video.titleVideo }}</td>
            </tr>
            <tr>
                <th>SubtitleVideo</th>
                <td>{{ video.subtitleVideo }}</td>
            </tr>
            <tr>
                <th>CategoryVideo</th>
                <td>{{ video.categoryVideo }}</td>
            </tr>
            <tr>
                <th>InfoVideo</th>
                <td>{{ video.infoVideo }}</td>
            </tr>
            <tr>
                <th>LinkVideo</th>
                <td>{{ video.linkVideo }}</td>
            </tr>
            <tr>
                <th>YearsVideo</th>
                <td>{{ video.yearsVideo ? video.yearsVideo|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('videos_index') }}">back to list</a>

    <a href="{{ path('videos_edit', {'id': video.id}) }}">edit</a>

    {{ include('videos/_delete_form.html.twig') }}
{% endblock %}
