{% extends 'baseAdmin.html.twig' %}

{% block title %}Admin paramétre{% endblock %}

{% block body %}
    <h1>MODIFIER LA DATE:</h1>

    {{ include('settings/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a class="backList" href="{{ path('settings_index') }}"><span>Retour à la liste</span></a>

    {# {{ include('settings/_delete_form.html.twig') }} #}
{% endblock %}
