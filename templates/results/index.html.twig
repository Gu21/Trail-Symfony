{% extends 'base.html.twig' %}

{% block title %}Results index{% endblock %}

{% block body %}
    <h1>Results index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>ImageResultat</th>
                <th>TitreResultat</th>      
                <th>LienResultat</th>
                <th>SousTitreResultat</th>
                <th>Modifier</th>
            </tr>
        </thead>
        <tbody>
        {% for result in results %}
            <tr>
                <td>{{ result.id }}</td>
                <td><img src="{{ asset('uploads/photos/' ~ result.pictureResult ) }}" width="100"></td>
                <td>{{ result.titleResult }}</td>
                <td>{{ result.linkResult }}</td>
                <td>{{ result.SubtitleResult}}</td>
                <td>
                    <a href="{{ path('results_show', {'id': result.id}) }}">show</a>
                    <a href="{{ path('results_edit', {'id': result.id}) }}"><i class="fas fa-pencil-alt"></i></a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('results_new') }}">Create new</a>

    	<script src="{{ asset('js/défilementFluide.js') }}"></script>
{% endblock %}
