{% extends 'baseAdmin.html.twig' %}

{% block title %}Admin modifier accueil | Trail des jambes Allaire
{% endblock %}

{% block body %}
	<h1>Modifier accueil</h1>


	{{ include('home/_form.html.twig', {'button_label': 'Modifier'}) }}

	<a class="backList" href="{{ path('home_index') }}">
		<span>Retour à la liste</span>
	</a>
	
	{% if  is_granted('ROLE_ADMIN') %}
		{{ include('home/_delete_form.html.twig') }}
	{% endif %}


{% endblock %}
