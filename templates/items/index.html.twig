{% extends 'base.html.twig' %}

{% block title %}Items index
{% endblock %}

{% block body %}
	{# <h1>Items index</h1>
			
			    <table class="table">
			        <thead>
			            <tr>
			                <th>Id</th>
			                <th>NameItem</th>
			                <th>CommentItem</th>
			                <th>DateItem</th>
			                <th>actions</th>
			            </tr>
			        </thead>
			        <tbody>
			        {% for item in items %}
			            <tr>
			                <td>{{ item.id }}</td>
			                <td>{{ item.nameItem }}</td>
			                <td>{{ item.commentItem }}</td>
			                <td>{{ item.dateItem ? item.dateItem|date('Y-m-d H:i:s') : '' }}</td>
			                <td>
			                    <a href="{{ path('items_show', {'id': item.id}) }}">show</a>
			                    <a href="{{ path('items_edit', {'id': item.id}) }}">edit</a>
			                </td>
			            </tr>
			        {% else %}
			            <tr>
			                <td colspan="5">no records found</td>
			            </tr>
			        {% endfor %}
			        </tbody>
			    </table>
			
			    <a href="{{ path('items_new') }}">Create new</a> #}




{% endblock %}
